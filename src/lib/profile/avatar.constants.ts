/** Square avatar output size in pixels. */
export const AVATAR_SIZE = 128;

/** Max accepted upload before client-side resize (5 MB). */
export const AVATAR_MAX_INPUT_BYTES = 5 * 1024 * 1024;

/** Max stored data-URL length (~24 KB payload). */
export const AVATAR_MAX_DATA_URL_LENGTH = 32_000;

export const AVATAR_ACCEPT = "image/jpeg,image/png,image/webp";
